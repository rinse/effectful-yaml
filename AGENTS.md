# AGENTS.md

effectful-yaml は、既存の YAML (JSON) パーサーの上で動くインタプリタフレームワークです。
YAML の文法内で独自の記法を定め、それを評価して新しい YAML を返したり、フレームワーク利用者の定義する副作用を起こしたりします。
YAML の配列を do 文のように解釈し、中で利用しているアクションに応じてその文全体の Effect が決まる設計を構想しています。
標準的な Effect はフレームワークが提供し、使用者が独自の Effect を定義することで利用できるアクションが増える構想です。

## ドキュメント

- [ドキュメント索引](docs/index.md)：`docs/` 配下の全ドキュメントへの入口。
- [言語仕様（草案 0.3）](docs/grammar.md)：DSL の文法と評価モデルの規範。
- `docs/reference/`：予約キーごとのリファレンス。

## ドキュメント管理

- `README.md`：プロジェクトの概要・目的など最重要事項のみを書きます。詳細は書かず `docs/` へのリンク集にします。
- `docs/`：人間向けドキュメントを置きます。
- `knowledge/`：エージェント向けドキュメント（knowledge bundle 等）を置きます。
