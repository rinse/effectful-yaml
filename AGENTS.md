# AGENTS.md

effectful-yaml は、既存の YAML (JSON) パーサーの上で動くインタプリタフレームワークです。
`$` で始まるキーを持つマッピングだけを計算として解釈し、それ以外は素の YAML のまま値になります。
ドットを含む `$` キーは環境にある値（関数か演算）の呼び出しで、ドットを含まない `$` キーは言語が予約する構文です。名前空間は環境であり、`std` もホストが与える名前も初期環境の束縛です。
カーネルは参照 `${...}`・`$let`・`$if`・`$fn`・`$handler` の 5 形で、`$do`・`$for`・`$default` と `std` の関数はその展開で定まります。ハンドラは構文 `$handler` で立て、再利用するハンドラは本体の閉包を受け取る関数（`$handler: ${std.list}`）です。

## プロジェクトの原則

1. このプロジェクトの標語は minimal but general です。最低限の仕様で最大限の表現力と使い心地を得ることを最優先します。
2. このプロジェクトでは仕様が先、実装が後です。ad-hoc な仕様追加はせず、常に最小限の仕様を考え直します。
3. エラー文言は、導出形や糖衣構文の展開先ではなく、利用者が書いた形を名乗ります（`$for 'x'`、`$std.where`）。展開が置いた演算は名乗る形（`what`）を持ち回り、評価器はそれを文言に使います。

## ドキュメント

- [ドキュメント索引](docs/index.md)：`docs/` 配下の全ドキュメントへの入口。
- [言語仕様（草案 0.14）](docs/grammar/index.md)：DSL の文法と評価モデルの規範。
- [処理系の利用](docs/usage.md)：TypeScript から評価器を呼び出す API と評価オプション。
- [CLI（eff-yaml）](docs/cli.md)：文書を評価するコマンド。`--ops` でホストの演算と関数を渡せる。
- `docs/reference/`：予約キーと、`std` の演算・関数ごとのリファレンス。

## ドキュメント管理

- `README.md`：プロジェクトの概要や目的など最重要事項のみを書きます。詳細は書かず `docs/` へのリンク集にします。
- `docs/`：人間向けドキュメントを置きます。
- `knowledge/`：エージェント向けドキュメント（knowledge bundle 等）を置きます。
- 導出形と糖衣構文（`std` の関数を含む）は、言語仕様とリファレンスにその展開を必ず載せます。展開が規範であり、実装は展開と観測的に等価でなければなりません。
