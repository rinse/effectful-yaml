# std.where

`std.where` は条件を満たさない分岐を打ち切る。
リスト内包表記のガードに相当する。

- 種別：標準演算
- 作用：選択

## 形

```yaml
- $std.where: 条件式
```

## 規則

- 条件が偽なら、その分岐は打ち切られて結果に何も残さない（null が残るのではない）。真なら何もしない。
- 条件は真偽値でなければならない。
- 文としての値は null である。
- 打ち切りは失敗ではない。`std.fail` が全体を失敗させるのに対し、`std.where` は分岐を静かに消す。
- `$std.mapping` の本体で使えば、条件を満たすエントリだけを持つマッピングが作れる。

## 例

```yaml
$do:
- $let:
    x: {$std.each: [1, 2, 3]}
    y: {$std.each: [1, 2, 3]}
- $std.where: ${x < y}
- - ${x}
  - ${y}
```

```yaml
[[1, 2], [1, 3], [2, 3]]
```

## 関連

- [std.each](std.each.md)
- [std.fail](std.fail.md)（打ち切りと失敗の違い）
- [std.mapping](std.mapping.md)（エントリの条件付き省略）
- [$if](if.md)（打ち切りでなく値の分岐が必要なとき）
